// Proxy server file to redirect to the actual server location
// This exists because package.json expects server/index.ts but actual server is at public/server/index.ts

import "../public/server/index";